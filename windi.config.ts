import { defineConfig } from 'windicss/helpers'

export default defineConfig({
    darkMode: 'class',
    shortcuts: {
        'button-primary': 'bg-purple-600 px-3 py-1 rounded-lg text-light-50 mr-5 hover:bg-purple-500'
    }
})